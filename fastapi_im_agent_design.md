# 基于 FastAPI 的 IM 工单 Agent 设计建议

结合你当前环境：

1. 工作流系统已开发完成
2. 已有 RAGFlow，历史工单可导入知识库
3. 有本地大模型

结论先说：**FastAPI 可以做“定时轮询 IM”**，但在生产里推荐优先使用 **Webhook 事件驱动**，轮询作为兜底。

## 1. FastAPI 能否定时轮询 IM？

可以。常见方式有三种：

- **APScheduler**：在 FastAPI 进程内启动定时任务，每 N 秒轮询 IM API。
- **Celery Beat + Worker**：Beat 负责定时触发，Worker 拉取消息并处理，稳定性更好。
- **系统级 Cron**：由系统调度脚本调用你的 FastAPI 内部接口或任务入口。

> 小规模、MVP：可先用 APScheduler。  
> 生产规模：建议 Celery Beat + Redis（或你现有调度平台）。

## 2. 推荐总体架构（贴合你现状）

- **IM Adapter（接入层）**：
  - Webhook 接收消息（首选）
  - Poller 轮询未读消息（兜底）
- **Orchestrator（FastAPI + 状态机）**：
  - 意图识别
  - 槽位抽取
  - 置信度评估
  - 多轮追问
- **RAG 检索层（RAGFlow）**：
  - 根据用户部门/岗位/历史相似单推荐字段值
- **LLM 推理层（本地模型）**：
  - 生成草稿
  - 解析用户修正指令（如“有效期改 90 天”）
- **Workflow Connector**：
  - 将确认后的结构化 payload 提交到现有工作流
- **治理层**：
  - 审计日志、幂等键、超时恢复、人工转接

## 3. 交互策略（关键）

采用“**先猜测，再确认，不确定就追问**”的字段级策略：

- `confidence >= 0.85`：自动填充
- `0.6 ~ 0.85`：展示在确认卡片让用户一键确认/修改
- `< 0.6`：只追问关键缺失字段（一次最多 1~3 个问题）

这样能减少用户输入负担，又避免盲目自动提交。

## 4. 轮询实现的工程注意点

如果你用轮询，务必加上：

- **游标/offset**：只拉取新消息，避免重复消费
- **幂等键**：`im_msg_id + user_id + scene`
- **分布式锁**：多实例下防止重复轮询
- **退避重试**：IM API 限流时指数退避
- **消息去重表**：处理过的消息落库

## 5. 实战建议

- 能拿到 IM 事件推送能力时：**优先 Webhook + ACK + 异步处理**
- 不能推送时：使用“**短周期轮询 + 去重 + 幂等**”
- 你的最佳组合建议：
  - FastAPI（API/回调）
  - Celery（异步任务）
  - Redis（队列 + 锁）
  - PostgreSQL（会话状态 + 审计）
  - RAGFlow（历史工单召回）
  - 本地模型（抽取、补全、对话）

## 6. 最小可用闭环（MVP）

1. IM 消息进入（Webhook 或 Polling）
2. 抽取工单字段并用 RAGFlow 补全候选
3. 给用户发确认卡片（确认/修改/转人工）
4. 用户确认后调用已有工作流提交
5. 回传工单号到 IM
6. 全链路写审计日志

## 7. 是否所有流程都允许自动创建？建议：只允许白名单

是的，你的判断非常关键：**不应该所有工作流都允许自动创建**。应先做“流程准入控制”，只对允许的流程执行自动建单。

建议把流程分级：

- **A类（可全自动）**：低风险、字段稳定、历史样本充足
- **B类（半自动）**：可生成草稿，但必须用户确认后提交
- **C类（禁止自动）**：高风险/高权限/合规敏感，只能转人工或跳转标准入口

### 7.1 对话阶段先做“流程识别 + 准入校验”

在 IM 首轮消息后，先执行：

1. 识别用户意图对应哪个工作流
2. 检查是否在 `allowed_workflow_list`
3. 按流程级别决定下一步动作

- 命中 A 类：可继续自动补全并提交确认卡
- 命中 B 类：仅生成草稿并明确“需你确认后提交”
- 命中 C 类或未命中：拒绝自动创建，给人工入口/表单链接

### 7.2 建议增加策略配置（可运营）

建议维护一张可配置策略表（数据库或配置中心）：

- `workflow_id`
- `workflow_name`
- `auto_create_mode`（auto / draft_only / forbidden）
- `required_user_confirm`（true/false）
- `sensitive_fields`（如权限级别、数据范围）
- `allowed_departments`
- `risk_level`

这样业务侧可自行调整哪些流程可自动化，而不必频繁改代码。

### 7.3 IM 交互文案建议

当流程不在允许列表时，明确反馈：

- “该流程暂不支持自动建单，我已为你打开人工提交流程：<链接>”
- “该流程涉及高权限操作，需人工审批，我可以先帮你整理草稿。”

这能避免用户误解“Agent 能处理所有流程”。
